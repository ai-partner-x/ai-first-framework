# 菜单 API

## 获取菜单树

**GET** `/api/menus/tree`

## 获取菜单列表

**GET** `/api/menus`

## 获取菜单详情

**GET** `/api/menus/:id`

## 创建菜单

**POST** `/api/menus`

## 更新菜单

**PUT** `/api/menus/:id`

## 删除菜单

**DELETE** `/api/menus/:id`
